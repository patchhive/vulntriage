version: "3.8"

services:
  backend:
    build: ./backend
    ports:
      - "8080:8000"
    volumes:
      - ./data:/data
    environment:
      - VULN_TRIAGE_DB_PATH=/data/vuln-triage.db
      - VULN_TRIAGE_PORT=8000
      - RUST_LOG=info
    env_file:
      - .env
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "5181:80"
    depends_on:
      - backend
    restart: unless-stopped
